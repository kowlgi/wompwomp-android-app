<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- customize here -->
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:actionBarStyle">@style/WompwompActionBarStyle</item>
        <!--For compatibility-->
        <item name="windowActionBarOverlay">true</item>
    </style>

    <style name="WompwompActionBarStyle" parent="AppTheme">
        <item name="android:background">@color/wompwompblue</item>
        <!-- color of title text -->
        <item name="android:textColorPrimary">@android:color/white</item>
        <!-- color of the menu overflow icon (three vertical dots) -->
        <item name="android:textColorSecondary">@android:color/white</item>

        <!--For compatibility-->
        <item name="background">@color/wompwompblue</item>
    </style>

    <style name="FullscreenTheme" parent="AppTheme">
        <item name="android:actionBarStyle">@style/FullscreenActionBarStyle</item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="windowActionBarOverlay">true</item>
        <item name="android:windowBackground">@null</item>

        <item name="android:colorPrimaryDark" tools:targetApi="lollipop">@android:color/transparent</item>
        <item name="colorPrimaryDark">@android:color/transparent</item>

        <item name="android:windowContentOverlay">@null</item>
    </style>

    <style name="FullscreenActionBarStyle" parent="FullscreenTheme">
        <item name="android:background">@android:color/transparent</item>
        <item name="android:colorPrimary" tools:targetApi="lollipop">@android:color/transparent</item>
        <item name="colorPrimary" tools:targetApi="lollipop">@android:color/transparent</item>
        <!-- color of the up button -->
        <item name="android:textColorSecondary">@android:color/white</item>
        <!--For compatibility-->
        <item name="background">@android:color/transparent</item>
    </style>
</resources>
